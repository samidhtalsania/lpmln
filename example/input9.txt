Dice = {D1, D2}
Boolean = {T,F}
Number = {1,2,3,4}

Even(Dice, Boolean)
Roll(Dice, Number)
InterveneRoll(Dice)

Roll(d,2) => Even(d,T).
Even(d, F) => Even(d, F).
!InterveneRoll(d) ^ !Roll(d,2) ^ !Roll(d,3) ^ !Roll(d,4) => Roll(d,1).

